{
  "name": "google-ads-negative-keyword-sweeper-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "codex:one-off": "tsx scripts/codex-subscription-one-off.ts",
    "kimi:one-off": "tsx scripts/kimi-subscription-one-off.ts",
    "test": "node --import tsx --test test/**/*.test.ts",
    "sweep": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
