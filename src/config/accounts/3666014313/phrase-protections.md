# Art City Auto Body — DG Enterprise — account phrase protections

No company-specific phrase exceptions are approved for customer 3666014313.
The agency-wide phrase protections still apply. Add only reviewed, narrowly
scoped evidence exceptions to the JSON array below; a match must never disable
an entire rule or automatically force KEEP.

```json
[]
```
